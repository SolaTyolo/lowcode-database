# Mode: single-tenant or multi-tenant
# TENANT_MODE=single
# TENANT_MODE=multi
TENANT_MODE=single

# Single-tenant DSN (used when TENANT_MODE=single)
# If not set, falls back to DATABASE_URL
SINGLE_DATABASE_URL=postgresql://postgres:postgres@0.0.0.0:5432/tables

# Generic DSN (optional, used as fallback for single-tenant mode)
DATABASE_URL=postgresql://postgres:postgres@0.0.0.0:5432/postgres

# Multi-tenant DSN template (used when TENANT_MODE=multi)
# %s will be replaced with tenant_id, e.g.:
#   postgresql://postgres:postgres@0.0.0.0:5432/<tenant_id>
TENANT_DSN_TEMPLATE=postgresql://postgres:postgres@0.0.0.0:5432/%s

# Server addresses
GRPC_ADDR=:9090
HTTP_ADDR=:8080

# Maximum rows returned per ListRows call (default & upper bound)
# MAX_ROW=100

